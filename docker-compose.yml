services:
  backend:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - DB_URL=jdbc:postgresql://db:5432/perfect_market
      - DB_USERNAME=${DB_USERNAME:-postgres}
      - DB_PASSWORD=${DB_PASSWORD:-postgres}
      - REDIS_HOST=redis
      - JWT_SECRET=${JWT_SECRET:-change-this-secret-key-at-least-32-chars}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-placeholder}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET:-placeholder}
      - FACEBOOK_CLIENT_ID=${FACEBOOK_CLIENT_ID:-placeholder}
      - FACEBOOK_CLIENT_SECRET=${FACEBOOK_CLIENT_SECRET:-placeholder}
      - MAIL_HOST=${MAIL_HOST:-smtp.gmail.com}
      - MAIL_PORT=${MAIL_PORT:-587}
      - MAIL_USERNAME=${MAIL_USERNAME:-noreply@perfectmarket.com}
      - MAIL_PASSWORD=${MAIL_PASSWORD:-}
      - FRONTEND_URL=${FRONTEND_URL:-http://localhost:3000}
      - OAUTH2_REDIRECT_BASE_URI=${OAUTH2_REDIRECT_BASE_URI:-http://localhost:8080}
      - SPRING_FLYWAY_OUT_OF_ORDER=true
      - SPRING_FLYWAY_VALIDATE_ON_MIGRATE=false
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - perfect-network

  db:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=perfect_market
      - POSTGRES_USER=${DB_USERNAME:-postgres}
      - POSTGRES_PASSWORD=${DB_PASSWORD:-postgres}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-postgres} -d perfect_market"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - perfect-network

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    networks:
      - perfect-network

  ngrok:
    image: ngrok/ngrok:latest
    restart: unless-stopped
    ports:
      - "4040:4040"
    env_file:
      - .env
    entrypoint: ["/bin/sh", "-c"]
    command: ["ngrok http backend:8080 --url=https://$${NGROK_DOMAIN} --log=stdout"]
    depends_on:
      - backend
    networks:
      - perfect-network

networks:
  perfect-network:
    driver: bridge

volumes:
  pgdata:
